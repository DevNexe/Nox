# NoxGram

NoxGram is a small Telegram bot library for Nox.

It provides a polling bot, command handlers, message handlers, custom filters,
basic middleware support, and a simple per-chat state store.

Use NoxGram when you want to build Telegram bots in pure Nox with a compact and
easy to read API.
