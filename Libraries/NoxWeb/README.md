# NoxWeb

NoxWeb is a lightweight web library for Nox applications.

It provides a small app and blueprint API for registering GET and POST routes,
serving static files, rendering templates, and returning text, JSON, HTML, or
custom responses.

Use NoxWeb when you want to build a simple local web app or documentation site
directly in Nox without pulling in a larger framework.
