# Types

Core types: numbers, strings, booleans, lists, tuples, dicts, sets.

```
n = 42
pi = 3.14
name = "nox"
flag = true
items = [1, 2, 3]
point = (1, 2)
map = {"a": 1}
setv = {1, 2, 3}
```

## Indexing

```
items[0]
items[1] = 42
```