# Syntax

## Blocks

Blocks are defined by indentation (spaces only). Tabs are not allowed.

```
if x > 0:
    display("positive")
else:
    display("non-positive")
```

## Comments

```
# This is a comment
```