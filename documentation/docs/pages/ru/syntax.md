# Синтаксис

## Блоки

Блоки задаются отступами (только пробелы). Табуляции запрещены.

```
if x > 0:
    display("positive")
else:
    display("non-positive")
```

## Комментарии

```
# Это комментарий
```